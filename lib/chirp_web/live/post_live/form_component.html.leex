<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "post-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :username %>
  <%= text_input f, :username %>
  <%= error_tag f, :username %>

  <%= label f, :body %>
  <%= text_input f, :body %>
  <%= error_tag f, :body %>

  <%= label f, :likes_cout %>
  <%= number_input f, :likes_cout %>
  <%= error_tag f, :likes_cout %>

  <%= label f, :reports_count %>
  <%= number_input f, :reports_count %>
  <%= error_tag f, :reports_count %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
